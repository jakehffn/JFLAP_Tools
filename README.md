# RegEx to JFLAP Finite Automaton

A command line tool for converting a simple Regular Expression into a finite automaton

For information on how to use the commnad line tool, use ```-h``` or ```--help```.
